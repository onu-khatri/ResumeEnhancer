# ResumeEnhancer.Core.WebLibrary Project

This project contains ASP.NET Core-specific helpers shared by web-facing modules.

Use this project for common endpoint execution, HTTP result shaping, and request/header helpers that depend on ASP.NET Core types. Keep framework-neutral helpers in `Core/CommonLibrary`.

