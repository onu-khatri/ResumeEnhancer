# ResumeModuleWeb Project

This project is intended for Resume module web-facing endpoints.

Keep transport concerns here, such as controllers or minimal APIs. Domain entities belong in `ResumeModuleDM`, service/use-case code belongs in `ResumeModuleSL`, and EF Core configuration belongs in `ResumeModulePL`.
